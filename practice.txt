def suma(int ref a, int ref b) -> int {
    int r = a + b;
    return(r);
}

def printSuma(int a, int b) -> void {
    int c = a + b;
    print(c);
    return;
}

def testAST() -> void {
    int x = 10;
    int y = 3;
    int z = 0;

    z = suma(x, y);     // func con return
    print(z);

    printSuma(x, y);    // func void

    print(esMayor(x, y));  // func dentro de print

    return;
}